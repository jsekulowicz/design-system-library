---
"@jsekulowicz/ds-components": minor
---

Segmented control: segment buttons no longer force `--ds-font-size-sm` via `::part(button)`; they now use the button's own `--ds-font-size-md`, consistent with `ds-button` and the table's md default.
