---
'@jsekulowicz/ds-components': minor
---

ds-select and ds-searchable-select options accept a `disabledReason` — shown as a hover tooltip and exposed to screen readers (announced when the option is reached by keyboard), so a disabled option can explain why it can't be picked.
