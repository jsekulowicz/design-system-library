---
'@jsekulowicz/ds-components': minor
---

New `ds-text-area`: a multi-line text input that shares the styling, label/description/error, and native form participation of `ds-text-field`. A `rows` prop sizes the field to exactly that many text rows.
