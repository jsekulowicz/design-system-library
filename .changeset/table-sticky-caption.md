---
'@jsekulowicz/ds-components': patch
---

`ds-table`: the caption now stays fixed at the start during horizontal scroll instead of scrolling off with the (wider-than-viewport) table body. It still wraps within the container in the stacked responsive layout.
