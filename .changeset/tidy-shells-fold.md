---
'@jsekulowicz/ds-components': patch
---

Keep the prerendered `ds-page-shell` mobile fallback below 768px so its initial layout matches the component's runtime breakpoint without exposing desktop side navigation.
