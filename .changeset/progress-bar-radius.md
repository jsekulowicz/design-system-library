---
"@jsekulowicz/ds-components": patch
---

Use `--ds-radius-sm` instead of `--ds-radius-full` for the progress bar track and fill, keeping the right side of the fill square until progress reaches 100%.
