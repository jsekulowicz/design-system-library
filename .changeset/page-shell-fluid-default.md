---
"@jsekulowicz/ds-components": patch
---

Default PageShell to a fluid content width. PageShell and SettingsPage stories now use the fluid default, with one PageShell story explicitly showing the capped layout opt-in.
