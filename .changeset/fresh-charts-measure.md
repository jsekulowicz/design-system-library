---
'@jsekulowicz/ds-components': patch
---

Remeasure `ds-bar-chart` after its loading skeleton is replaced so the rendered chart uses the container width without stretching its SVG content. Keep `ds-stat-tile` loading values in the same line box as loaded values to prevent layout shifts.
