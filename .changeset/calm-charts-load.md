---
"@jsekulowicz/ds-components": minor
"@jsekulowicz/ds-react": minor
---

Add `ds-stat-tile` with value loading and hint slots, and add skeleton loading states to `ds-bar-chart` and `ds-heatmap-calendar`.
