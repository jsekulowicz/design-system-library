---
"@jsekulowicz/ds-components": patch
---

Align `ds-sidenav` and mobile drawer spacing with page-shell chrome, and add a `drawer-brand` slot for mobile drawer branding while preserving compact horizontal padding in collapsed mode.
