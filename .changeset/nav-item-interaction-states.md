---
"@jsekulowicz/ds-components": minor
---

NavItem: keep disabled links keyboard-focusable and add a focusable `loading` state with busy semantics and a spinner. Disabled standalone controls now consistently remain keyboard-focusable and expose `aria-disabled`.
