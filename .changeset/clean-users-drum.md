---
"@jsekulowicz/ds-components": patch
---

Add the `user-circle` icon export, keep mobile menu triggers as bars icons while open, order mobile `ds-page-shell` header content as brand, menu toggle, then header actions, and hide `ds-navbar`'s mobile toggle when no links are slotted.
