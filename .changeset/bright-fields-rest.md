---
'@jsekulowicz/ds-components': minor
---

Make reserved field-message space opt-in through `message-space` on every component that renders field subtext. Descriptions and errors still render without opting in, while empty fields no longer add a blank message row by default.
