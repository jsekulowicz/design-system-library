---
'@jsekulowicz/ds-components': patch
---

`ds-table`: the horizontal scroll container no longer rubber-band bounces or chains to the page (`overscroll-behavior-x: none`), matching the vertical behaviour already used in `scroll-body` mode.
