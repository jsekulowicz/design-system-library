---
"@jsekulowicz/ds-components": minor
---

Table: default font size is now `--ds-font-size-md` (was `--ds-font-size-sm`), matching the base body size. Slotted cell content that inherits font size — including progress-bar labels — scales with it.
