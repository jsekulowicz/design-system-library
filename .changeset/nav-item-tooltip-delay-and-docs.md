---
'@jsekulowicz/ds-components': minor
---

NavItem: add the `compactHoverTooltipDelay` prop and `compact-hover-tooltip-delay` attribute for configuring the compact-mode hover tooltip delay. Storybook API pages now show Props tables for NavItem, NavGroup, Breadcrumb, Tabs, and BarChart components.
