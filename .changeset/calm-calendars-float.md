---
'@jsekulowicz/ds-components': patch
---

Keep heatmap tooltips visible outside the horizontal scroller, include the legend in the padded focus frame, and prevent bar-chart tooltip labels from wrapping away from their series indicator.
