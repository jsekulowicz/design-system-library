---
'@jsekulowicz/ds-components': patch
---

ds-searchable-select now truncates a long selected value with an ellipsis instead of hard-clipping it (the input still scrolls normally while focused and typing).
